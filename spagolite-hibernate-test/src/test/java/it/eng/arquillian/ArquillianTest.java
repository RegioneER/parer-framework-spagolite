package it.eng.arquillian;

/*import javax.inject.Inject;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;

import org.jboss.arquillian.container.test.api.Deployment;
import org.jboss.arquillian.junit.Arquillian;
import org.jboss.shrinkwrap.api.ShrinkWrap;
import org.jboss.shrinkwrap.api.asset.EmptyAsset;
import org.jboss.shrinkwrap.api.spec.JavaArchive;
*/

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author mbertuzzi
 */

// @RunWith(Arquillian.class)
public class ArquillianTest {
    /*
     * @Deployment public static JavaArchive createDeployment() { JavaArchive jar =
     * ShrinkWrap.create(JavaArchive.class).addClass(Greeter.class); System.out.println(jar.toString(true)); return jar;
     * }
     * 
     * @Inject Greeter greeter;
     * 
     * @Test public void should_create_greeting() { Assert.assertEquals("Hello, Earthling!",
     * greeter.createGreeting("Earthling")); greeter.greet(System.out, "Earthling"); }
     */

}
